# 当前实现与验收证据

> 更新日期：2026-07-30。原工作包中的“默认状态”是规划时初始值，不代表当前代码状态；当前验收以本文件、仓库根 `docs/full-scope-completion-matrix.md`、自动化测试和外部平台记录为准。

## 已有可验证能力

- OIDC PKCE、项目级 RBAC、服务主体、PostgreSQL RLS 与 9 个前向迁移。
- Edge Collector 初始化、断点哈希缓存、签名 Bundle、离线 `.tar.zst`、受控运行、资源上限和大目录性能门禁。
- 无 Git 基线、文本脱敏 diff、二进制摘要、移动/复制候选和冷热快照保留。
- GitHub REST/Webhook 与允许目录内的本地 Git 兼容采集。
- 持久后台导入状态机、租约恢复、指数退避、修正重试和不可变对象载荷。
- 确定性谱系/审计、人工边审核、结果状态提案和版本历史。
- ADK 单入口 Agent、只读工具、安全边界、轨迹评测和模型不可用降级。
- Workspace Drive/Sheets/Gmail draft-only 流程、预览确认、外部幂等和步骤恢复。
- Web 控制台（含可直接浏览相邻关系和 evidence ID 的节点详情）、OpenAPI 3.1、Prometheus/OTel、告警、备份恢复、威胁模型和依赖风险登记。
- 固定 SHA 的 CI、跨平台/E2E/RLS 门禁、CycloneDX/Sigstore 发布证据，以及 GitHub OIDC 到 Cloud Run 的受保护 CD。

## 本机最新证据

- Collector：17/17 测试通过；5,000 文件门禁覆盖冷/热扫描、缓存 100% 命中和目录指纹稳定，吞吐读数保存在每次门禁日志中。
- 浏览器：6/6 E2E 通过，覆盖 10 个页面、Axe serious/critical、Manifest 上传、发现解决、交接预览和新用户关系证据导航。
- 本地 Git：2/2 路径隔离与有界树采集测试通过。
- OpenAPI：42 个 `/v1` 操作完成契约校验。
- 幂等：20 个写路由全部受门禁保护。
- 数据库：9 个迁移、18 个租户表通过静态/RLS 校验。
- Terraform 1.15.8：Google provider 7.41.0 配置初始化并验证通过。

## 必须由外部环境完成的验收

- 真实 Cloud SQL、OIDC、GCS、GitHub App、Google Workspace OAuth 和模型项目的沙箱联调与撤权测试。
- 目标科研存储上的百万文件性能验收。
- Cloud Run 首次部署、迁移、readiness、镜像回滚和备份恢复演练。
- 获得数据授权的真实课题组试点、业务指标、培训签到和 RC 签字。

外部证据缺失时不得把对应项目标为“完整完成”，也不得用演示数据替代。
